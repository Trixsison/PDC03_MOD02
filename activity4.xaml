<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PDC3_MOD2.activity4" Title="Data Binding Mode and Grid">
    <ContentPage.Content>
        <StackLayout>
            <Frame BackgroundColor="Aqua" Padding="15" CornerRadius="0">
                <Slider VerticalOptions="CenterAndExpand" ValueChanged="OnSliderValueChanged"/>
            </Frame>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
               
                </Grid.ColumnDefinitions>

                <Label Text="NAME" TextColor="Black"/>
                <Label Text="COURSE"
                       Grid.Row="0" Grid.Column="1" TextColor="Black"/>


                <Label Text="Trixie Sison"
                       Grid.Row="0" Grid.Column="1"/>
                <Label Text="BS in Information Technology"
                       Grid.Row="0" Grid.Column="1"/>

            </Grid>

            <Button Text="Back" Clicked="BackPage"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>